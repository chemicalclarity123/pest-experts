---
import Layout from '../../layouts/Layout.astro';
import LeadFormCompact from '../../components/LeadFormCompact.astro';
import Testimonials from '../../components/Testimonials.astro';
import FAQ from '../../components/FAQ.astro';
import WhyChooseUs from '../../components/WhyChooseUs.astro';
import WhatsAppWidget from '../../components/WhatsAppWidget.astro';
import { PortableText } from "astro-portabletext";
import { 
  fetchServices, 
  fetchServiceBySlug, 
  fetchCompanySettings, 
  fetchHomepage,
  urlFor 
} from '../../lib/sanity-client';
import { generateServiceSchema, generateFAQSchema } from '../../lib/schema';
import Footer from '../../components/Footer.astro';
import Breadcrumbs from '../../components/Breadcrumbs.astro';

export async function getStaticPaths() {
  const services = await fetchServices();
  return services.map((service: any) => ({
    params: { slug: service.slug },
  }));
}

const { slug } = Astro.params;
const service = await fetchServiceBySlug(slug as string);

// Lanza queries independientes en paralelo para reducir TTFB
const [companySettings, homepage] = await Promise.all([
  fetchCompanySettings(),
  fetchHomepage(),
]);

if (!service) {
  return Astro.redirect('/404');
}

// Fallback Logic
const testimonials = service.customTestimonials?.reviews?.length > 0 
  ? service.customTestimonials 
  : (companySettings?.testimonials?.reviews?.length > 0 ? companySettings.testimonials : homepage?.testimonials);

const faqs = service.localContext?.faqs?.length > 0
  ? service.localContext.faqs
  : homepage?.faq?.questions;

const faqTitle = service.localContext?.faqs?.length > 0
  ? "Common Questions"
  : homepage?.faq?.title;

const whatsappNumber = companySettings?.contactInfo?.whatsapp || "+27123456789";

// Imagen hero — se necesita antes del schema
const heroImage = service.image?.asset 
  ? urlFor(service.image).width(1920).height(800).url() 
  : '/images/hero-bg.jpg';
  
// Schema Generation — incluye imagen y URL para rich results en Google
const serviceSchema = generateServiceSchema({
  name: service.title,
  description: service.description,
  serviceType: service.localContext?.serviceType || "PestControl",
  image: heroImage,
  url: Astro.url.href,
  provider: {
    name: companySettings?.businessName || "Built For Speed Pest Experts",
    url: import.meta.env.SITE || "https://pestexperts.co.za",
  },
  areaServed: service.localContext?.areas || companySettings?.serviceAreas || ["Pretoria", "Johannesburg"],
  knowsAbout: service.localContext?.pests,
  priceRange: service.pricing?.priceRange || "$$",
});

const faqSchema = faqs ? generateFAQSchema({ faqs }) : null;

// Combine Schemas
const schemaMarkup = [
    serviceSchema,
    ...(faqSchema ? [faqSchema] : [])
];

// Get key features from Sanity
const keyFeatures = service.features || [];

/* Genera un heading SEO dinámico para la sección de contenido completo */
const fullContentHeading = `Professional ${service.title} in Gauteng`;

// Breadcrumb Items
const breadcrumbItems = [
  { name: 'Home', item: Astro.site?.toString() || '/' },
  { name: 'Services', item: new URL('/services', Astro.site || 'https://pest-experts-site.pages.dev').href },
  { name: service.title, item: Astro.url.href }
];
---

<Layout 
  seo={service.seo}
  schemaMarkup={schemaMarkup}
>
  <!-- Hero Section with 2-Column Layout -->
  <section class="hero-section">
    <!-- Background Image Container -->
    <div class="hero-background">
      <img 
        src={heroImage} 
        alt={service.image?.alt || service.title}
        class="hero-image"
        width="1920"
        height="800"
        fetchpriority="high"
        decoding="async"
      />
      <div class="hero-overlay"></div>
    </div>

    <!-- Hero Content -->
    <div class="content-container hero-container">
      <div class="hero-grid">
        <!-- Left Column: Content -->
        <div class="hero-content">
          <!-- Breadcrumb for SEO -->
          <Breadcrumbs items={breadcrumbItems} />

          <h1 class="hero-title">{service.title}</h1>
          
          {service.hero?.subheading && (
            <p class="hero-subheading">{service.hero.subheading}</p>
          )}

          <!-- Key Features from Sanity -->
          {keyFeatures.length > 0 && (
            <ul class="key-features">
              {keyFeatures.map((feature: string) => (
                <li>
                  <svg class="check-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7" />
                  </svg>
                  <span>{feature}</span>
                </li>
              ))}
            </ul>
          )}

          <!-- Trust Badges -->
          <div class="trust-badges">
            <div class="badge">
              <svg class="badge-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
              </svg>
              <span>Licensed & Insured</span>
            </div>
            <div class="badge">
              <svg class="badge-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              <span>Fast Response</span>
            </div>
            <div class="badge">
              <svg class="badge-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4M7.835 4.697a3.42 3.42 0 001.946-.806 3.42 3.42 0 014.438 0 3.42 3.42 0 001.946.806 3.42 3.42 0 013.138 3.138 3.42 3.42 0 00.806 1.946 3.42 3.42 0 010 4.438 3.42 3.42 0 00-.806 1.946 3.42 3.42 0 01-3.138 3.138 3.42 3.42 0 00-1.946.806 3.42 3.42 0 01-4.438 0 3.42 3.42 0 00-1.946-.806 3.42 3.42 0 01-3.138-3.138 3.42 3.42 0 00-.806-1.946 3.42 3.42 0 010-4.438 3.42 3.42 0 00.806-1.946 3.42 3.42 0 013.138-3.138z" />
              </svg>
              <span>Guaranteed Results</span>
            </div>
          </div>

          <!-- CTA Buttons -->
          <div class="hero-cta-buttons">
            <a 
              href={"tel:" + (companySettings?.contactInfo?.telephone || "")}
              class="cta-primary"
            >
              <svg class="cta-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
              </svg>
              Call {companySettings?.contactInfo?.telephone}
            </a>
            <a 
              href="#lead-form" 
              class="cta-secondary"
            >
              Get Free Quote
            </a>
          </div>
        </div>

        <!-- Right Column: Lead Form -->
        <div class="hero-form" id="lead-form">
          <LeadFormCompact 
            title="Get a Free Quote"
            description="Fast response across Gauteng"
            source={`service_${service.slug}`}
            formId="serviceHeroForm"
          />
          
          <!-- Quick Stats -->
          <div class="quick-stats">
            <div class="stat">
              <div class="stat-value" style="display: flex; align-items: center; justify-content: center; gap: 6px;">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48" width="20px" height="20px">
                <path fill="#FFC107" d="M43.611,20.083H42V20H24v8h11.303c-1.649,4.657-6.08,8-11.303,8c-6.627,0-12-5.373-12-12c0-6.627,5.373-12,12-12c3.059,0,5.842,1.154,7.961,3.039l5.657-5.657C34.046,6.053,29.268,4,24,4C12.955,4,4,12.955,4,24c0,11.045,8.955,20,20,20c11.045,0,20-8.955,20-20C44,22.659,43.862,21.35,43.611,20.083z"/>
                <path fill="#FF3D00" d="M6.306,14.691l6.571,4.819C14.655,15.108,18.961,12,24,12c3.059,0,5.842,1.154,7.961,3.039l5.657-5.657C34.046,6.053,29.268,4,24,4C16.318,4,9.656,8.337,6.306,14.691z"/>
                <path fill="#4CAF50" d="M24,44c5.166,0,9.86-1.977,13.409-5.192l-6.19-5.238C29.211,35.091,26.715,36,24,36c-5.202,0-9.619-3.317-11.283-7.946l-6.522,5.025C9.505,39.556,16.227,44,24,44z"/>
                <path fill="#1976D2" d="M43.611,20.083H42V20H24v8h11.303c-0.792,2.237-2.231,4.166-4.087,5.571c0.001-0.001,0.002-0.001,0.003-0.002l6.19,5.238C36.971,39.205,44,34,44,24C44,22.659,43.862,21.35,43.611,20.083z"/>
              </svg>
              {companySettings?.aggregateRating?.ratingValue || "4.8"} <span style="color: #FFC107;">★</span>
            </div>
              <div class="stat-label">Google Rating</div>
            </div>
            <div class="stat">
              <div class="stat-value">{new Date().getFullYear() - 2004}<span style="color: #FFC107;">+</span></div>
              <div class="stat-label">Years Experience</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Main Content -->
  <div class="content-container section-spacing">
    <!-- Contenido principal del servicio desde Sanity (campo "Full Content") -->
    {service.content?.length > 0 && (
      <section class="content-section">
        <div class="cro-intro-card bg-white p-8 md:p-12 rounded-2xl shadow-sm border border-gray-100 max-w-4xl mx-auto text-center relative overflow-hidden">
          
          {/* Subtle background flair */}
          <div class="absolute top-0 right-0 w-64 h-64 bg-green-50 rounded-full blur-3xl opacity-50 -z-10 -translate-y-1/2 translate-x-1/2"></div>
          
          {/* Trust Signal Badge */}
          <div class="inline-flex items-center gap-2 px-5 py-2 rounded-full bg-green-50 border border-green-100 text-sm font-bold text-green-900 mb-6 shadow-sm transition-transform hover:scale-105">
            <span class="flex items-center justify-center w-6 h-6 rounded-full bg-white shadow-sm text-base">🛡️</span> 
            <span class="tracking-wide uppercase text-xs">Certified {service.title} Experts</span>
          </div>

          <h2 class="section-heading mt-0 mb-6 text-left">
            {fullContentHeading}
          </h2>
          
          <div class="prose prose-lg prose-green area-intro-prose max-w-none text-left text-gray-600">
            <PortableText value={service.content} />
          </div>

          <div class="mt-12">
            <a href="#lead-form" class="cta-primary inline-flex text-lg px-8 py-4 w-full sm:w-auto justify-center shadow-lg hover:shadow-xl hover:-translate-y-1 transition-all duration-300">
              Get a Free Quote Now
            </a>
          </div>
        </div>
      </section>
    )}

    <!-- Overview -->
    {service.overview && (
      <section class="content-section overview-section">
        <div class="bg-white p-8 md:p-12 rounded-2xl shadow-sm border border-gray-100 max-w-4xl mx-auto relative overflow-hidden">
          <h2 class="section-heading mt-0 mb-6 text-left">
            {service.overviewHeading || "Why Choose Professional Pest Control?"}
          </h2>
          <div class="prose prose-lg prose-green area-intro-prose max-w-none text-left text-gray-600">
            <PortableText value={service.overview} />
          </div>
        </div>
      </section>
    )}

    <!-- Signs of Infestation -->
    {service.signsOfInfestation?.length > 0 && (
      <section class="content-section max-w-4xl mx-auto">
        <h2 class="section-title">
          <span class="title-icon">⚠️</span>
          Signs of Infestation
        </h2>
        <div class="signs-grid">
          {service.signsOfInfestation.map((sign: string) => (
            <div class="sign-card">
              <svg class="sign-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
              </svg>
              <span>{sign}</span>
            </div>
          ))}
        </div>
      </section>
    )}

    <!-- Treatment Process -->
    {service.treatmentProcess?.length > 0 && (
      <section class="content-section max-w-4xl mx-auto">
        <h2 class="section-title">Our Treatment Process</h2>
        <div class="process-timeline">
          {service.treatmentProcess.map((step: any, index: number) => (
            <div class="process-step">
              <div class="step-number">{index + 1}</div>
              <div class="step-content">
                <h3 class="step-title">{step.stepTitle}</h3>
                <p class="step-description">{step.stepDescription}</p>
              </div>
            </div>
          ))}
        </div>
      </section>
    )}

    <!-- Benefits -->
    {service.benefits?.length > 0 && (
      <section class="content-section benefits-section">
        <h2 class="section-title">Why Professional Treatment?</h2>
        <div class="bg-white p-8 md:p-12 rounded-2xl shadow-sm border border-gray-100 max-w-4xl mx-auto">
          <div class="prose prose-lg max-w-none">
            <PortableText value={service.benefits} />
          </div>
        </div>
      </section>
    )}
  </div>

  <!-- Why Choose Us -->
  <WhyChooseUs 
    title={homepage?.whyChooseUs?.title} 
    features={homepage?.whyChooseUs?.features} 
  />

  <!-- FAQ Section -->
  {faqs && (
    <div class="faq-section">
      <div class="content-container">
        <FAQ 
          title={faqTitle}
          questions={faqs}
        />
      </div>
    </div>
  )}

  <!-- Testimonials -->
  {testimonials && (
    <Testimonials 
      title={testimonials.title || "Success Stories"}
      reviews={testimonials.reviews}
      googleReviews={companySettings?.googleReviews}
    />
  )}

  <WhatsAppWidget 
    phoneNumber={whatsappNumber}
    message={`Hi! I'd like to get a quote for ${service.title}.`}
  />

  <Footer slot="footer" companyData={companySettings} />
</Layout>

<style>
  /* Hero Section */
  .hero-section {
    position: relative;
    min-height: 600px;
    display: flex;
    align-items: center;
    padding: 6rem 0;
    overflow: hidden;
  }



  .hero-background {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .hero-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(43, 107, 42, 0.1) 0%, rgba(43, 107, 42, 0.1) 50%, rgba(43, 107, 42, 0.1) 100%);
  }

  .hero-container {
    position: relative;
    z-index: 10;
  }

  .hero-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    align-items: start;
  }

  @media (min-width: 1024px) {
    .hero-grid {
      grid-template-columns: 1.2fr 0.8fr;
      gap: 4rem;
    }
  }

  /* Breadcrumb */
  .breadcrumb {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.875rem;
    color: white;
    margin-bottom: 1.5rem;
  }

  .breadcrumb a {
    color: white;
    text-decoration: none;
    transition: color 0.2s;
  }

  .breadcrumb a:hover {
    color: white;
    text-decoration: underline;
  }

  .breadcrumb span:last-child {
    color: white;
    font-weight: 600;
  }

  /* Hero Content */
  .hero-content {
    color: white;
    background: rgba(0, 0, 0, 0.5);
    padding: 2.5rem;
    border-radius: 16px;
    backdrop-filter: blur(10px);
  }

  .hero-title {
    font-size: 2.5rem;
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 1rem;
    color: white;
  }

  @media (min-width: 768px) {
    .hero-title {
      font-size: 3.5rem;
    }
  }

  @media (min-width: 1024px) {
    .hero-title {
      font-size: 4rem;
    }
  }

  .hero-subheading {
    font-size: 1.25rem;
    line-height: 1.6;
    color: white;
    margin-bottom: 2rem;
  }

  /* Key Features */
  .key-features {
    list-style: none;
    padding: 0;
    margin: 0 0 2rem 0;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .key-features li {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 1.125rem;
    font-weight: 500;
    color: white;
  }

  .check-icon {
    width: 24px;
    height: 24px;
    flex-shrink: 0;
    color: #fbbf24;
  }

  /* Trust Badges */
  .trust-badges {
    display: flex;
    flex-wrap: wrap;
    gap: 1.5rem;
    margin-bottom: 2rem;
  }

  .badge {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.875rem;
    font-weight: 600;
    color: white;
  }

  .badge-icon {
    width: 20px;
    height: 20px;
    color: #fbbf24;
  }

  /* CTA Buttons */
  .hero-cta-buttons {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .cta-primary,
  .cta-secondary {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 1rem 2rem;
    border-radius: 12px;
    font-weight: 700;
    font-size: 1.125rem;
    text-decoration: none;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .cta-primary {
    background: white;
    color: #2b6b2a;
  }

  .cta-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.2);
  }

  .cta-secondary {
    background: rgba(255, 255, 255, 0.1);
    color: white;
    border: 2px solid white;
    backdrop-filter: blur(10px);
  }

  .cta-secondary:hover {
    background: rgba(255, 255, 255, 0.2);
    transform: translateY(-2px);
  }

  .cta-icon {
    width: 20px;
    height: 20px;
  }

  /* Hero Form */
  .hero-form {
    position: sticky;
    top: 2rem;
  }

  .quick-stats {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    margin-top: 1.5rem;
  }

  .stat {
    background: white;
    padding: 1rem;
    border-radius: 12px;
    text-align: center;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  }

  .stat-value {
    font-size: 1.5rem;
    font-weight: 700;
    color: #2b6b2a;
    margin-bottom: 0.25rem;
  }

  .stat-label {
    font-size: 0.75rem;
    color: #6b7280;
    font-weight: 600;
  }

  /* Content Sections */
  .content-section {
    margin-bottom: 4rem;
  }

  .section-title {
    font-size: 2rem;
    font-weight: 700;
    color: #2b6b2a;
    margin-bottom: 2rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    text-align: center;
  }

  .title-icon {
    font-size: 2rem;
  }

  /* Signs Grid */
  .signs-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1rem;
  }

  @media (min-width: 768px) {
    .signs-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .sign-card {
    background: white;
    padding: 1.5rem;
    border-radius: 12px;
    box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1);
    border: 1px solid #e5e7eb;
    display: flex;
    align-items: start;
    gap: 1rem;
    transition: all 0.2s;
  }

  .sign-card:hover {
    border-color: #2b6b2a;
    box-shadow: 0 10px 15px -3px rgba(43, 107, 42, 0.1);
    transform: translateY(-2px);
  }

  .sign-icon {
    width: 24px;
    height: 24px;
    flex-shrink: 0;
    color: #f97316;
    margin-top: 0.125rem;
  }

  .sign-card span {
    color: #374151;
    font-weight: 500;
  }

  /* Process Timeline */
  .process-timeline {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    position: relative;
  }

  .process-timeline::before {
    content: '';
    position: absolute;
    left: 23px;
    top: 40px;
    bottom: 40px;
    width: 2px;
    background: #dcfce7;
  }

  @media (max-width: 767px) {
    .process-timeline::before {
      left: 19px;
    }
  }

  .process-step {
    display: flex;
    gap: 2rem;
    position: relative;
  }

  .step-number {
    width: 48px;
    height: 48px;
    flex-shrink: 0;
    background: #2b6b2a;
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.25rem;
    font-weight: 700;
    box-shadow: 0 0 0 4px white;
    position: relative;
    z-index: 10;
  }

  .step-content {
    flex: 1;
    background: #f9fafb;
    padding: 1.5rem;
    border-radius: 12px;
    border: 1px solid #e5e7eb;
    transition: all 0.2s;
  }

  .step-content:hover {
    border-color: #2b6b2a;
    background: white;
  }

  .step-title {
    font-size: 1.25rem;
    font-weight: 700;
    color: #2b6b2a;
    margin-bottom: 0.5rem;
  }

  .step-description {
    color: #6b7280;
    line-height: 1.6;
  }

  /* Benefits Section */
  .benefits-section {
    background: #f0fdf4;
    padding: 2rem;
    border-radius: 16px;
    border: 1px solid #dcfce7;
  }

  /* FAQ Section */
  .faq-section {
    background: #f9fafb;
    padding: 4rem 0;
  }

  @media (min-width: 1024px) {
    .faq-section {
      padding: 6rem 0;
    }
  }

  /* Responsive adjustments */
  @media (max-width: 1023px) {
    .hero-section {
      min-height: auto;
      padding: 4rem 0;
    }

    .hero-form {
      position: static;
    }
  }

  @media (max-width: 640px) {
    .hero-title {
      font-size: 2rem;
    }

    .hero-subheading {
      font-size: 1.125rem;
    }

    .key-features li {
      font-size: 1rem;
    }

    .trust-badges {
      gap: 1rem;
    }

    .badge {
      font-size: 0.8125rem;
    }

    .cta-primary,
    .cta-secondary {
      width: 100%;
      justify-content: center;
      padding: 0.875rem 1.5rem;
      font-size: 1rem;
    }

    .section-title {
      font-size: 1.5rem;
    }

    .step-number {
      width: 40px;
      height: 40px;
      font-size: 1.125rem;
    }
  }

  /* Prose styling for PortableText */
  .prose {
    color: #374151;
    line-height: 1.75;
  }

  .prose p {
    margin-bottom: 1.25rem;
  }

  .prose h3 {
    font-size: 1.5rem;
    font-weight: 700;
    color: #2b6b2a;
    margin-top: 2rem;
    margin-bottom: 1rem;
  }

  .prose ul {
    list-style-type: disc;
    padding-left: 1.625rem;
    margin-bottom: 1.25rem;
  }

  .prose li {
    margin-bottom: 0.5rem;
  }

  .prose strong {
    font-weight: 600;
    color: #1f2937;
  }

  /* Overview Section Styling */
  .overview-section {
    background: white;
    padding: 2.5rem;
    border-radius: 12px;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
  }



  /* Increase font size for overview text to make it stand out */
  .overview-section .prose {
    font-size: 1.125rem;
    line-height: 1.8;
    color: #4b5563;
  }
</style>
